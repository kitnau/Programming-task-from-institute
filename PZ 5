#include <iostream>
#include <string>
using namespace std;

string check_bracket(char inner_symbol, int &flag1, int &flag2, int &flag3){
    if (inner_symbol ==  '(') flag1 = 1;
    else if (inner_symbol ==  '[') flag2 = 1;
    else if (inner_symbol ==  '{') flag3 = 1;
    if (inner_symbol ==  ')') {
        if (flag1 == 0) return "NO";
        flag1 = 0;
    }
    else if (inner_symbol ==  ']') {
        if (flag2 == 0) return "NO";
        flag2 = 0;
    }
    else if (inner_symbol ==  '}') {
        if (flag3 == 0) return "NO";
        flag3 = 0;
    }
    
    return "YES";
}

int main()
{
    string stroka;
    int flag1 = 0; // отвечает за проверку скобок ()
    int flag2 = 0; // отвечает за проверку скобок []
    int flag3 = 0; // отвечает за проверку скобок {}
    string flag_error = "YES";
    cin >> stroka;
    for (int i = 0; i < sizeof(stroka); i++) {
        if (check_bracket (stroka[i], flag1, flag2, flag3) == "NO") {
            flag_error = "NO";
            break;
        }
        }
    if ((flag1 or flag2 or flag3) == 1) flag_error = "NO";
    cout << flag1 << " " << flag2 << " " << flag3 << "\n";
    cout << flag_error;
    return 0;
}
